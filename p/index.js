const text = document.querySelector('.text')

text.addEventListener('input', (e) => {
    let word = ""
    word += e.target.value
    console.log(e)

    if (e.target.value == "") {
        
    }
})
